class Solution:
    def buildArray(self, target: List[int], n: int) -> List[str]:
        l = []
        m = max(target)
        for i in range(1,m+1):
            if(i in target):
                l.append('Push')
            else:
                l.append('Push')
                l.append('Pop')
        return l
        
